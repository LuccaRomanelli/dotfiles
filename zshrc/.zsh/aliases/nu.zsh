## @cmd nu
## @desc Nu CLI tooling (run `help nu <subcmd>` for details)

# No aliases defined here — this file serves as documentation
# that nu CLI commands exist and are available via nucli.
# Use `help nu <subcmd>` to get help for any nu subcommand.
